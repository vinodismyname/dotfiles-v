###############################################################################
# Editor Configuration
# Sets the default text editor for the shell environment
###############################################################################

# Set VS Code as the default editor
# --wait flag ensures VS Code waits for the file to be closed before returning
export EDITOR="code --wait"
export VISUAL="$EDITOR"
